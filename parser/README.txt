Seguem as modificações que haviamos feito e que estavam interferindo com o
código enviado anteriormente:

<coloca aqui as funções modificadas>

Assim os seguintes casos teste apresentam uma saída diferente daqueles
descritos no relatório. Segue o output atualizado:

1.Função fatorial:

  entrada:

        define f(n) {
                if (n == 0) return 1
                else return n * f(n - 1)
        }

  saída:

        Analise sintatica completa.

2.Declaração de função:

  entrada:

        define f() {
                auto ai
                ai = 2
                return (ai)
        }

  saída:

        Analise sintatica completa.

3.Aritmética simples:

  entrada:

        ai = 2
        -2 * ++ai

  saída:

        Analise sintatica completa.

Muito obrigado por considerar essa modificação, o grupo todo agracede a
ompreensão.
